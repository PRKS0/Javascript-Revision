"use strict"; // treat our code as newer version

const Name = 'hacker hero'  // unchangable 
let age = 18                // changable
var address = 'kathmandu'   // changable
// main difference between let and var is var is changable in every scope(block and functional) and it create problem.
// So let is only changable within a scope

let salary                  // when we only define the variable but not asign value then it's value is undefine.
//tabular method to console data
console.table([Name, age, address, salary]);

